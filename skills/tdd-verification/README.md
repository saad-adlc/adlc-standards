# tdd-verification (vendored block)

Doc 9 names a single vendored **"TDD/verification"** building block for *behavioral discipline*. It maps to **two** skills from `obra/superpowers` (MIT), vendored here unchanged:

| Sub-skill | Purpose |
|---|---|
| `test-driven-development/` | Enforces the red→green→refactor cycle: a failing test before any production code. Includes `testing-anti-patterns.md`. |
| `verification-before-completion/` | Requires the agent to verify its work actually satisfies the goal before declaring done. |

**Upstream:** `obra/superpowers` · **License:** MIT (see `LICENSE`) · **Pin:** see `../../VENDOR.md`.

These are fork-and-own copies. Do not edit them directly to "fix" upstream behavior — instead let the vendor-sync bot surface upstream changes as a PR, then adapt. Local ADLC-specific overrides (if ever needed) belong in the constitution/steering, not in these vendored files.
